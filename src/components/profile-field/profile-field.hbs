<div class="profile-field{{#if className}} {{ className }}{{/if}}">
  <div class="profile-field">{{ title }}</div>
  {{#if edit}}
  <div class="profile-field__value">{{ title }}</div>
  {{else}}
  <div class="profile-field__value">{{ value }}</div>
  {{/if}}
</div>
